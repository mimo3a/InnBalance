/**
 * Statistics Tab
 * 
 * Entry point for the statistics tab that renders the StatisticScreen.
 * Displays user's breathing exercise history and analytics.
 */

import StatisticScreen from '@/src/screens/StatisticScreen';

/**
 * StatisticsTab Component
 * Wrapper for the StatisticScreen in the tab navigation
 */
export default function StatisticsTab() {
  return <StatisticScreen />;
}
