/**
 * Settings Tab
 * 
 * Entry point for the settings tab that renders the SettingsScreen.
 * Users can manage their data and app preferences here.
 */

import SettingsScreen from '@/src/screens/SettingsScreen';

/**
 * SettingsTab Component
 * Wrapper for the SettingsScreen in the tab navigation
 */
export default function SettingsTab() {
  return <SettingsScreen />;
}